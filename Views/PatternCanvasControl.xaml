<UserControl x:Class="KnitStichGrid.Views.PatternCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="500">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Border Margin="0,12,12,0"
            Padding="12"
            BorderBrush="#D3D3D3"
            BorderThickness="1"
            CornerRadius="8">
        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Grid Width="{Binding PreviewGridWidthPx}"
                  Height="{Binding PreviewGridHeightPx}"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  SnapsToDevicePixels="True"
                  UseLayoutRounding="True">
                <ItemsControl Panel.ZIndex="0">
                    <ItemsControl.ItemsSource>
                        <Binding Path="PreviewCells" />
                    </ItemsControl.ItemsSource>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <primitives:UniformGrid Rows="{Binding GridRows}"
                                                    Columns="{Binding GridColumns}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Width="{Binding DataContext.CellWidthPx, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          Height="{Binding DataContext.CellHeightPx, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          IsChecked="{Binding IsFilled, Mode=TwoWay}">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="#FFFFFF" />
                                        <Setter Property="BorderBrush" Value="#BDBDBD" />
                                        <Setter Property="BorderThickness" Value="0.75" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0" />
                                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            SnapsToDevicePixels="True" />
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter Property="Background" Value="#2B2B2B" />
                                                <Setter Property="BorderBrush" Value="#CFCFCF" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Image Panel.ZIndex="1"
                       Source="{Binding OverlayImageSource}"
                       Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVisibility}}"
                       Opacity="{Binding OverlayOpacity}"
                       Stretch="Uniform"
                       SnapsToDevicePixels="True"
                       UseLayoutRounding="True"
                       IsHitTestVisible="False"
                       RenderOptions.BitmapScalingMode="NearestNeighbor" />
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
